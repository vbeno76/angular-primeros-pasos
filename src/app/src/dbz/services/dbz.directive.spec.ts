import { DbzDirective } from './dbz.directive';

describe('DbzDirective', () => {
  it('should create an instance', () => {
    const directive = new DbzDirective();
    expect(directive).toBeTruthy();
  });
});
